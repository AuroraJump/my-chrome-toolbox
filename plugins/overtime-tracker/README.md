# 加班工时实时统计 v2.4.6 - Chrome 扩展

## 安装
1. `chrome://extensions/` 打开开发者模式
2. 加载已解压 → 选本目录
3. 打开考勤页

## 加班规则(可在插件图标点开调整)

### 工作日
- **排班模式**:
  - `弹性` (默认): 加班 = max(0, 实际工时 − 目标工时 − 门槛)
  - `固定`: 加班 = max(0, 实际下班 − 标准下班 − 门槛)(与实际上班时间无关)
- **固定模式**: 上班时间 + 下班时间 (默认 09:00 → 18:00)
- **弹性模式**: 每日目标工时 (默认 9h)
- **加班门槛**: 延后超过此值才算加班, 否则 0 (默认 2h)

### 周末 / 节假日
- 不分弹性/固定模式, 统一规则:
  - 工时 ≥ `weekendThreshold` (默认 4h) 才算加班
  - 单日封顶 `weekendMax` (默认 8h)

### 交通补贴(按天计,不算时长)
- **工作日**: 实际下班 ≥ `subsidyStartTime` (默认 21:00,含整点) → **1 次/天**
- **周末/节假日**: 工时 ≥ 4h → **1 次/天**; 超过 8h 也只算 1 次补贴

### 算法示例 (threshold=2h, baseHours=9h, workEndTime=18:00)

| 打卡 | 弹性模式 | 固定模式 |
|------|----------|----------|
| 07:48-21:01 | 4h13m OT | 3h01m OT |
| 07:52-18:10 | 0 (未达门槛) | 0 (未达门槛) |
| 09:00-19:30 | 1h30m OT | 1h30m OT |

### 特殊日
- 法定节假日(无打卡) / 调休 / 年假 / 病假 / 事假 / 婚假 / 丧假 / 产假 / 陪产假 / 工伤假 / 未打卡 → 加班 = 0
- 6/1 等"系统误标节假日"但有实际打卡的 → 按工作日计算

## 悬浮窗功能
- 📊 实时显示本月累计加班
- 📈 目标完成度进度条
- 🔥 超过 100% 红色高亮 + 脉动动画
- 📅 可展开每日明细
- 🚕 交通补贴次数(显示在主面板, 明细里每条 OT 末尾 `· 🚕×N`)
- ⏰ 该打卡了下班提醒(工作日有上班无下班时显示)
- 🖱️ 标题栏拖拽
- ➕➖ 默认迷你浮窗 / 完整面板
- ↔ 右侧停靠模式
- 🏷️ 右上角 OT 红标(诊断标记, 点开显示版本号)

## 版本历史
- **v2.4.6** (2026-07-03): 调整周末/节假日规则: 满 4h 才算加班, 单日时长封顶 8h, 补贴最多 1 次/天
- **v2.4.5** (2026-07-03): 优化多月统计结果 UI, 改为两列指标卡、紧凑摘要行和无横向滚动的月度表
- **v2.4.4** (2026-07-03): 优化加班规则配置面板, 改为卡片分组、统一表单控件与主次按钮样式
- **v2.4.3** (2026-07-03): 修复月份范围输入在窄面板下被原生 month 控件撑开的问题, 改为上下两行表单布局
- **v2.4.2** (2026-07-03): 统一多月统计控件区 UI, 调整预设按钮、月份输入和统计按钮的尺寸、边框、间距与视觉层级
- **v2.4.1** (2026-07-03): 优化浮窗 UI 质感, 调整按钮为展开 `⛶` / 收起 `▁`, 替代不够明确的 `+/-`
- **v2.4.0** (2026-07-03): 优化浮窗体验——默认迷你浮窗、完整面板切换、右侧停靠模式; 多月统计改成指标卡 + 月度表格, 增加最高/最低月份
- **v2.3.6** (2026-07-03): 修复多月统计翻月方向反了的问题; 历史月份结果改为按时间升序计算、最近月份置顶展示; 抓取完成后会回到结束月
- **v2.3.5** (2026-07-02): 多月统计重做——把 "近 N 月" 下拉改成 [从 X / 到 Y] 月份范围选择 + 4 个预设按钮 (本月/近3月/近6月/近12月). 抓取顺序改为: 先翻到结束月 → 往回抓到开始月 → 停在结束月. navigateToMonth 安全差值从 24 提到 60 月
- **v2.3.4** (2026-07-02): 修多月统计 off-by-1 bug——#Year/#Month input 滞后显示 1 个月, 月份标签全错位. 改用从日历标题文本 (例 "2026 年 7 月") 解析年月, 跟显示始终同步; input 作为兜底
- **v2.3.3** (2026-07-02): 修 oa.aciic.cn Prev 按钮 text "< Prev" 没匹配上(加 exact 模式 + 部分 regex 加 `<`/`>`) + 翻月加安全检查(差值 > 24 中止 + 点击后年月未变重试一次)
- **v2.3.2** (2026-07-02): 修多月统计的 3 个 bug——(1) 抓取结果被 render() 覆盖 (加 aggregateCache 缓存) (2) 抓完不跳回当月 (加 navigateToMonth 反向翻月) (3) oa.aciic.cn 上 Prev 按钮找不到 (findNavButton 加更多匹配模式: Prev/上月/Previous 等)
- **v2.3.1** (2026-07-02): 修 aggregate 按钮点击无反应——之前用 querySelector 直接挂监听, 但按钮在 body.innerHTML 里每次 render() 重建, 监听丢了; 改成事件委托. 同时把月份选择移到悬浮面板(select: 本月/3/6/12), 实时持久化到 chrome.storage.sync
- **v2.3.0** (2026-07-02): 多月统计——可指定统计月数(默认 12=近一年), 点 panel 按钮自动翻月份抓数据, 显示累计/月均/本月对比/每月明细
- **v2.2.5** (2026-07-01): 修复只打上班没打下班时 reminder 不显示——老正则要求两边都匹配, "08:02:14 - " 这种只有上班时整条不匹配, 改成 split 后两侧各自匹配
- **v2.2.4** (2026-07-01): 下班提醒修两个 bug——(1) 没有完整打卡时早退导致不显示提醒 (2) 阈值从 -30 拉到 -60 + 加 normal 档(上班时也有提示)
- **v2.2.3** (2026-07-01): URL 白名单加 path 前缀, 屏蔽同域名其他页面误触发 (如 `oa.aciic.cn/home/...`)
- **v2.2.2** (2026-07-01): 修复 soa.com.cn HH:MM:SS 格式解析(老正则贪婪匹配 MM:SS, 跳过 HH)
- **v2.2.1** (2026-07-01): URL 白名单写死两个考勤域 (`soa.com.cn` + `oa.aciic.cn`)
- **v2.2.0** (2026-06-29): 排班模式(固定/弹性)、周末/节假日规则、交通补贴(按天)、固定模式工作日 OT 从 18:00 起算
- **v2.0.0**: 月度加班统计 + 浮动面板

## 文件
- `manifest.json` - 扩展配置(版本号)
- `content.js` - 核心: 读日历 + 计算 + 渲染
- `content.css` - 悬浮窗样式
- `popup.html / popup.js / popup.css` - 规则配置弹窗
- `diagnose.js` - 诊断脚本(控制台粘贴运行)
- `icons/icon-128.png` - 工具栏图标
